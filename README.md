Nettsted for ZWEB Digitalbyrå

## Contact Form Email

Set these environment variables in your deployment:

- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USER`
- `SMTP_PASS`
- `SMTP_SECURE` (optional, defaults to `true` when port is 465)
- `CONTACT_TO`
- `CONTACT_FROM` (optional, defaults to `SMTP_USER`)
